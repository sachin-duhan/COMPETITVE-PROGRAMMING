#include <bits/stdc++.h>
using namespace std;
string test(int X, int Y, int arr[])
{
    if (X == Y)
    {
    }
    else
    {
    }
}
int main()
{
    int t;
    cin >> t;
    while (t > 0)
    {
        int n;
        int hard;
        int easy;
        int arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
        sort(arr, arr + n);

        t--;
    }

    return 0;
}