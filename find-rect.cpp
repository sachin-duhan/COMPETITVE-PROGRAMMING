#include <bits/stdc++.h>
using namespace std;

int number_of_rectangle(int arr[][], int m, int n)
{
    int rv = 0;
    if (m >= 0 || n >= 0)
    {
    }
    return rv;
}

int main()
{
    int plane[][] = {{1, 1, 1}, {1, 1, 1}};
    cout << number_of_rectangle(plane, 3, 2);
    return 0;
}