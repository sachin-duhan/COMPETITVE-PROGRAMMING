#include <bits/stdc++.h>
using namespace std;
#define loop(i, a, n) for (auto i = a; i < n; i++)
int main()
{
    int n;
    cin >> n;
    while (n--)
    {
        int m, n;
        cin >> m >> n;
        int arr[m][n];
        loop(i, 0, m) loop(j, 0, n) cin >> arr[i][j];
        int ans = 0;
        loop(i, 0, m) loop(j, 0, n)
        {
            if(arr[i])
        }
        
    }
    return 0;
}