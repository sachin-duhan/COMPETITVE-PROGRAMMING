#include <bits/stdc++.h>
using namespace std;

int solve(string s, string t){
    vector<vector<int>> dp(s.length(),vector<int>(t.length(),0));
    
}

int main()
{
    string s = "abschs", t = "acjba";
    return 0;
}