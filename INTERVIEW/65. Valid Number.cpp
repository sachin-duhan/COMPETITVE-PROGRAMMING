#include <iostream>
using namespace std;

bool isNumber(string s)
{

    // Only e allowed!
    // +/- in front of string is allowed
    // Only numbers allowed!

    // will solve it some other day! LoL

}

int main()
{
    string s = "0.1";
    cout << isNumber(s) << endl;
    return 0;
}