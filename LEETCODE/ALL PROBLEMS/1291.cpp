#include <bits/stdc++.h>
using namespace std;
vector<int> sequentialDigits(int low, int high)
{
    vector<int> ans;
    
    return ans;
}

int main()
{
    vector<int> output = sequentialDigits(10, 10000);
    for (auto a : output)
        cout << a << " ";
    cout << endl;
    return 0;
}